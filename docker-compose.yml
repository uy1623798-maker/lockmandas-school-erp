services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lockmandas_erp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["erp_pgdata:/var/lib/postgresql/data"]
volumes:
  erp_pgdata:
