import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import cookieParser from 'cookie-parser'; import path from 'node:path'; import { env } from './config.js'; import { authRouter } from './routes/auth.js'; import { attendanceRouter } from './routes/attendance.js'; import { adminRouter } from './routes/admin.js'; import { portalRouter } from './routes/portal.js'; import { prisma } from './lib.js';
const app=express(); app.use(helmet()); app.use(cors({origin:env.WEB_URL,credentials:true})); app.use(express.json({limit:'1mb'})); app.use(cookieParser());
app.get('/api/health',(_q,r)=>r.json({status:'ok'})); app.use('/api/auth',authRouter); app.use('/api/attendance',attendanceRouter); app.use('/api/admin',adminRouter); app.use('/api/portal',portalRouter);
app.post('/api/public/admission',async(q,r,n)=>{try{r.status(201).json(await prisma.admissionEnquiry.create({data:q.body}))}catch(e){n(e)}}); app.post('/api/public/contact',async(q,r,n)=>{try{r.status(201).json(await prisma.contactMessage.create({data:q.body}))}catch(e){n(e)}});
if(process.env.NODE_ENV==='production'){const web=path.resolve(process.cwd(),'..','web','dist');app.use(express.static(web));app.get('*',(_q,r)=>r.sendFile(path.join(web,'index.html')))}
app.use((e:any,_q:any,r:any,_n:any)=>{console.error(e);r.status(e.name==='ZodError'?400:500).json({message:e.name==='ZodError'?'Please check the submitted details':'Something went wrong',issues:e.issues})}); app.listen(env.PORT,()=>console.log(`API ready on http://localhost:${env.PORT}`));
